const mailData = (state) => state.mailData;
const accessToken = (state) => state.accessToken;
const selectedMail = (state) => state.selectedMail;

export default {
  mailData,
  accessToken,
  selectedMail,
};
